# OpenCodeReview - GitHub Actions PR Auto-Review
#
# Reviews pull requests with Alibaba open-code-review (ocr) and posts the
# findings back onto the PR as inline review comments.
# Upstream: https://github.com/alibaba/open-code-review
#
# Triggers:
#   - PR opened or reopened
#   - A PR comment from a repo OWNER/MEMBER/COLLABORATOR beginning with
#     '/open-code-review' or '@open-code-review' (re-run on demand)
#
# Configuration:
#   Non-secret settings live in the job-level `env:` block below as plaintext
#   (OCR reads these env vars natively, with priority over any config file):
#     OCR_LLM_MODEL      - Model name
#     OCR_USE_ANTHROPIC  - false for OpenAI-compatible providers
#
# Required repository secrets:
#   OCR_LLM_URL        - OpenAI-compatible chat-completions endpoint
#                        (mapped to OCR_LLM_URL env on the review step)
#   OCR_LLM_AUTH_TOKEN - API key (mapped to OCR_LLM_TOKEN env on the review step)
#
# Note: GITHUB_TOKEN is provided automatically by GitHub Actions.

name: OpenCodeReview PR Review

on:
  pull_request:
    types: [opened, reopened]
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write

# Cancel duplicate/parallel runs for the same PR (open + rapid re-trigger comments).
concurrency:
  group: ocr-review-${{ github.event.issue.number || github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  code-review:
    runs-on: ubuntu-latest
    # Non-secret OCR config as plaintext env vars (read natively by ocr).
    env:
      OCR_LLM_MODEL: "gpt-4o"
      OCR_USE_ANTHROPIC: "false"
    # Run on PR open/reopen (same-repo only), or on a trigger comment from an
    # authorized member. issue_comment workflows run from the default branch WITH
    # secret access regardless of commenter, so the association gate below is
    # required to stop arbitrary users triggering a secret-bearing LLM job.
    # Fork PRs are skipped on the pull_request path: origin/<head> won't resolve
    # and secrets are empty, so they'd only produce a confusing error comment.
    if: |
      (github.event_name == 'pull_request' &&
       github.event.pull_request.head.repo.fork == false) ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request &&
       (startsWith(github.event.comment.body, '/open-code-review') || startsWith(github.event.comment.body, '@open-code-review')) &&
       contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association))
    steps:
      - name: Get PR context
        id: pr-context
        if: github.event_name != 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            // For issue_comment events, resolve the PR head/base refs.
            const prNumber = context.issue.number;
            const { data: pullRequest } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });
            core.setOutput('base_ref', pullRequest.base.ref);
            core.setOutput('head_ref', pullRequest.head.ref);
            core.setOutput('head_sha', pullRequest.head.sha);

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history needed for merge-base diff
          ref: ${{ github.event_name != 'pull_request' && steps.pr-context.outputs.head_sha || '' }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install OpenCodeReview
        run: npm install -g @alibaba-group/open-code-review

      - name: Run OpenCodeReview
        id: review
        env:
          # Endpoint + API key come from secrets; MODEL is job-level env above.
          OCR_LLM_URL: ${{ secrets.OCR_LLM_URL }}
          OCR_LLM_TOKEN: ${{ secrets.OCR_LLM_AUTH_TOKEN }}
        run: |
          # Resolve base/head refs (differ between PR and comment triggers).
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE_REF="${{ github.event.pull_request.base.ref }}"
            HEAD_REF="${{ github.event.pull_request.head.ref }}"
          else
            BASE_REF="${{ steps.pr-context.outputs.base_ref }}"
            HEAD_REF="${{ steps.pr-context.outputs.head_ref }}"
          fi

          echo "Reviewing PR: ${HEAD_REF} against ${BASE_REF}"

          # Range mode with machine-readable output for the posting step below.
          ocr review \
            --from "origin/${BASE_REF}" \
            --to "origin/${HEAD_REF}" \
            --format json \
            > /tmp/ocr-result.json 2>/tmp/ocr-stderr.log || true

          echo "OCR review completed. Output:"
          cat /tmp/ocr-result.json
          echo "OCR review completed. Error log:"
          cat /tmp/ocr-stderr.log

      - name: Post review comments to PR
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const path = '/tmp/ocr-result.json';

            // Read OCR output.
            let result;
            try {
              const raw = fs.readFileSync(path, 'utf8');
              result = JSON.parse(raw);
            } catch (e) {
              console.log('Failed to parse OCR output:', e.message);
              // Surface the error as a plain PR comment.
              const stderr = fs.readFileSync('/tmp/ocr-stderr.log', 'utf8').trim();
              if (stderr) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  body: `⚠️ **OpenCodeReview** encountered an error:\n\`\`\`\n${stderr}\n\`\`\``
                });
              }
              return;
            }

            const comments = result.comments || [];
            const warnings = result.warnings || [];

            // No findings -> post a short summary and stop.
            if (comments.length === 0) {
              const message = result.message || 'No comments generated. Looks good to me.';
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `✅ **OpenCodeReview**: ${message}`
              });
              return;
            }

            // Prepare PR review with inline comments.
            const prNumber = context.issue.number;
            let commitSha;

            if (context.eventName === 'pull_request') {
              commitSha = context.payload.pull_request.head.sha;
            } else {
              const { data: pullRequest } = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber
              });
              commitSha = pullRequest.head.sha;
            }

            // Only comments carrying line info can be posted inline via createReview.
            const reviewComments = [];
            const commentsWithoutLine = [];

            for (const comment of comments) {
              const body = formatComment(comment);

              const hasValidLine = (comment.start_line >= 1) || (comment.end_line >= 1);
              if (!hasValidLine) {
                commentsWithoutLine.push({ comment, body });
                continue;
              }

              const reviewComment = {
                path: comment.path,
                body: body
              };

              if (comment.start_line >= 1 && comment.end_line >= 1 && comment.start_line !== comment.end_line) {
                reviewComment.start_line = comment.start_line;
                reviewComment.line = comment.end_line;
                reviewComment.start_side = 'RIGHT';
                reviewComment.side = 'RIGHT';
              } else if (comment.end_line >= 1) {
                reviewComment.line = comment.end_line;
                reviewComment.side = 'RIGHT';
              } else if (comment.start_line >= 1) {
                reviewComment.line = comment.start_line;
                reviewComment.side = 'RIGHT';
              }

              reviewComments.push(reviewComment);
            }

            // Submit as a single PR review carrying all comments.
            const totalCount = comments.length;
            const inlineCount = reviewComments.length;
            const summaryCount = commentsWithoutLine.length;
            let summaryBody = `🔍 **OpenCodeReview** found **${totalCount}** issue(s) in this PR.`;
            if (totalCount > 0) {
              summaryBody += `\n- ✅ ${inlineCount} posted as inline comment(s)`;
              summaryBody += `\n- 📝 ${summaryCount} posted as summary (missing line info)`;
            }
            if (warnings.length > 0) {
              summaryBody += `\n\n⚠️ ${warnings.length} warning(s) occurred during review.`;
            }

            for (const { comment, body } of commentsWithoutLine) {
              summaryBody += '\n\n---\n\n';
              summaryBody += formatCommentMarkdown(comment);
            }

            let successCount = 0;
            let failedCount = 0;
            const failedComments = [];

            try {
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
                commit_id: commitSha,
                body: summaryBody,
                event: 'COMMENT',
                comments: reviewComments
              });
              successCount = reviewComments.length;
              console.log(`Successfully posted review with ${successCount} inline comments (${commentsWithoutLine.length} in summary)`);
            } catch (e) {
              console.log('Failed to post review with inline comments:', e.message);
              console.log('Falling back to posting comments individually...');

              for (const reviewComment of reviewComments) {
                try {
                  await github.rest.pulls.createReview({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    pull_number: prNumber,
                    commit_id: commitSha,
                    body: '',
                    event: 'COMMENT',
                    comments: [reviewComment]
                  });
                  successCount++;
                  console.log(`Successfully posted comment for ${reviewComment.path}`);
                } catch (innerE) {
                  failedCount++;
                  failedComments.push({ comment: reviewComment, error: innerE.message });
                  console.log(`Failed to post comment for ${reviewComment.path}: ${innerE.message}`);
                }
              }

              let finalBody = summaryBody;
              finalBody += `\n\n---\n\n📊 **Posting Statistics:**`;
              finalBody += `\n- ✅ Successfully posted: ${successCount} comment(s)`;
              if (failedCount > 0) {
                finalBody += `\n- ❌ Failed to post: ${failedCount} comment(s)`;
              }

              if (failedComments.length > 0) {
                finalBody += '\n\n<details><summary>❌ Failed Comments Details</summary>\n\n';
                for (const { comment, error } of failedComments) {
                  finalBody += `- \`${comment.path}\`: ${error}\n`;
                }
                finalBody += '\n</details>';
              }

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: finalBody
              });
            }

            function formatComment(comment) {
              let body = comment.content || '';

              if (comment.suggestion_code && comment.existing_code) {
                body += '\n\n**Suggestion:**\n';
                body += '```suggestion\n';
                body += comment.suggestion_code;
                if (!comment.suggestion_code.endsWith('\n')) body += '\n';
                body += '```';
              }

              return body;
            }

            function formatCommentMarkdown(comment) {
              let md = `### 📄 \`${comment.path}\``;
              if (comment.start_line && comment.end_line) {
                md += ` (L${comment.start_line}-L${comment.end_line})`;
              }
              md += '\n\n';
              md += comment.content || '';

              if (comment.suggestion_code && comment.existing_code) {
                md += '\n\n<details><summary>💡 Suggested Change</summary>\n\n';
                md += '**Before:**\n```\n' + comment.existing_code + '\n```\n\n';
                md += '**After:**\n```\n' + comment.suggestion_code + '\n```\n\n';
                md += '</details>';
              }

              return md;
            }
